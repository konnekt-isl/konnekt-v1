// Here we will show the authentication status.
// We will use the answer from the API.
import React, { Component } from 'react';
import * as firebase from 'firebase'
import { FirebaseContext } from './firebase';

